while True:
    password = input("Enter Password: ")

    lettercheck = any(char.isalpha() for char in password)
    numcheck = any(char.isdigit() for char in password)

    if lettercheck and numcheck:
        print("Password accepted.")
        break
    else:
        print("Invalid password! Try Again.")